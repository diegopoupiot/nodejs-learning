<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>NodeJS-learning</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <body class="p-5">
    <h1 class="mb-5 text-center">Modification</h1>
    <form action="" method="post">
        {% for index, value in member %}
      <div class="form-group mb-2">
        <label for="{{ index }}" class="form-label">{{ index }}</label>
        <input type="text" class="form-control" id="{{ index }}" name="{{ index }}" value="{{ value }}" {% if index == 'id' %} readonly {% endif %}>
      </div>
        {% endfor %}
      <button type="submit" class="btn btn-primary">Modifier</button>
    </form>
    <form action="/delete" method="post" class="mt-2">
       <input type="hidden" class="form-control" name="id" value="{{ member.id }}">
       <button type="submit" class="btn btn-secondary">Supprimer</button>
    </form>
    <a href="/" class="btn btn-secondary mt-2">Accueil</button>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

  </body>

</html>